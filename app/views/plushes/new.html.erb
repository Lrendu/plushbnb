<h1>New Plush</h1>

<%= simple_form_for(@plush) do |f| %>
<%= f.input :name %>
  <%= f.input :description %>
  <%= f.input :price %>
  <%= f.input :owner %>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>

<%= link_to 'Back', plushes_path %>
